package game.environments;


    /**
     * A factory class that manages the spawning for east and west sides of the map.
     * Created by: Vicky Huang
     * @author Vicky Huang
     * Modified by:
     *
     */
public class SpawnManager {

        // the method works out if the location is east or west


        // in SpawnableGround tick method:
    /*
    // we can get location's map
        this.map = location.map();

        // use the GameMap class 'getXRange' method to get the width of the map:
    // public NumberRange getXRange() {
    //		return widths;
    //	}

    // if x location <= width/2 (get the floor of the division), then it is west
    // if x location > width/2 (get the floor of the division), then it is east


        // get the location x and y coordinates
        this.x = location.x();
        this.y = location.y();
     */


}
